
#include "main.h"
/**
 * _islower - This checks the lowercase characters
 * @c: this are the  characters to check
 * Return: 1 if c is Lowercase, 0 otherwise
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}

