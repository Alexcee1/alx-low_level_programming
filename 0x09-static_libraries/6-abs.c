#include "main.h"
/**
 * _abs - This computes the absolute value of an integer
 * @n:this is the interger to check
 * Return: this is the absolute value of int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
